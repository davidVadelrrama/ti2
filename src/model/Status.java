package model;


/**
*This enum class establishes a status of consult for each pet registered.
*/

public enum status
{
	WAITING, IN_CONSULTATION, HOSPITALIZATION, DEPARTURE, DEPARTURE_WHITOUT_ATTENTION;
}